<h3>Discographie</h3>

<div class="col-12">
        <div class="row">
        {% if discs == [] %}
            <p>Il n'y a pas de d'album pour {{artist.name}}. Si vous voulez en ajouter une cliquez sur modifier {{artist.name}}</p>
        {% else %}

        {% for disc in discs %}
            <div class="col-4 py-1">
                <div class="row">
                    <div class="col-6">
                        <a href="{{path('disc_read', {id : disc.id})}}">
                            <img src="{{asset ('img/discs/' ~ disc.cover ~ '.png') }}" alt="{{disc.altImg}}" class="img-fluid">
                        </a>
                    </div>

                    <div class="col-6">
                        <a href="{{path('disc_read', {id : disc.id})}}">
                            <h3>
                                {{disc.name}}
                            </h3>
                            
                        </a>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
    {% endif %}
</div>
