<h3>Videos</h3>
{% if artist.videos is empty %}
    <p>Il n'y a pas de videos pour {{artist.name}}. Si vous voulez en ajouter une cliquez sur modifier {{artist.name}}</p>
{% else %}
<table class="table">   
    <thead>
        <th>Nom</th>
        <th>Type</th>
        <th>Lien</th>
    </thead>
    <tbody>
    {% for video in artist.videos %}
        <tr>
            <td>{{video.name}}</td>
            <td>{{video.category.name}}</td>
            <td><a href="{{video.url}}" target="_blank">{{video.url}}</a></td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endif %}